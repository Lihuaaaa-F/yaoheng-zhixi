# 本轮浏览器验收（范围限定）

## 结果

2026-09-15T18:48:47.842Z 至 2026-09-15T18:49:07.031Z，最终 **48 项浏览器自动检查全部通过**，控制台未记录应用错误。页面 URL：http://127.0.0.1:8765。

这仅是列出的浏览器功能与布局验收，不是整份报告业务验收。报告八维状态仍有模型未通过、章节实质完整与真人可读性/版式待评，默认页面明确显示“尚未合格／待评”。

## 环境与操作路径

- Browser 插件不可用，复用项目已有 Playwright 和已安装 Chrome；未安装新依赖、未修改全局环境。
- 1366×768、1920×1080、390×844；Linux/WSL 下实际浏览器运行。
- 四场景筛选：银黄口服液 2026-05、板蓝根颗粒 2026-05、六味地黄胶囊 2026-03、银黄口服液 2026-Q2（截至六月，按产量加权）。
- 筛选 → 指标/图表 → 跨厂方向交换 → 适用证据 → S2 实际生成 → Word/PDF/机器审计实际下载 → 载入真实建议 → 草稿/重复提交/编辑保存 → 历史记录 → 故障与恢复。
- 最终 S2 报告任务：bc4ae85bd50c4d58a51ad92045957472；实际草稿：YH-6fe91f816c2f4ebd9a7580c1，未确认、未发送。

## 已核验变化

- 默认报告与证据页面不再展示完整 JSON、哈希、metric_refs 或检索切片；折叠开发者详情保留原始机器审计。
- 八项验收分别显示；文件生成与整份报告合格分离。模型不可用显示基础分析和原因解释待复核。
- 同一快照的成本趋势、三要素结构、变动瀑布、跨厂柱图正常渲染；趋势鼠标提示实际可交互，桥接切换环比/同比/预算。
- 板蓝根材料贡献 78.26%/89.19%/78.72%，去年同期 4.42/1.04/1.64，原料 83.33%，跨厂 40%/26%/34%，二月 7.25 和五月止五个月趋势均独立断言。
- 检索默认结果不含银黄口服液或六味地黄胶囊，并实际显示生产工艺文档第 3 页。
- 只有包含实际建议的发现可载入任务；核查对象、预期证据、部门/角色、优先级和期限依据齐全。具体姓名默认待分配，建议日期可在草稿编辑时调整。
- 重复草稿复用身份；编辑后预期证据保存且确认身份变化；未点击确认发送。
- 1366/1920/390 未出现整页横向溢出。已查看截图中的数字、图表、表格、默认来源、验收与任务内容，未发现字体缺字或遮挡主要操作。

## 真实失败及修复记录

1. 沙箱 Chrome socket 被系统权限拒绝：记录失败后获准使用当前 WSL 浏览器运行。
2. 初始趋势断言误把月份下拉的未来候选项当成图表未来数据，已限定到实际趋势面板；初始总额测试值更正为源数据对应的 761,940 元。均属于测试断言修正，不改变计算。
3. 新生成报告在 RUNNING 阶段提前暴露 DOCX 下载，当 LibreOffice 更新目录及文件时浏览器下载取消。确认旧终态文件 GET 与浏览器下载正常后，前端改为仅 SUCCEEDED/DEGRADED 终态开放文件下载。显式使用 /tmp 浏览器下载目录，避免宿主临时目录干扰。
4. 编辑草稿后再次提交原建议触发后端任务编号冲突，主流程已增加独立身份与冲突处理回归；最终浏览器再次创建原建议、重复提交、编辑均通过。
5. 通知状态只由实际发送证明字段判断；不再从 received 推断 sent。

历史尝试逐行保存在 history.jsonl；后期截图带运行时间前缀，失败图不覆盖成功证据。旧 e2e 脚本保留作历史实现证据，其中“默认页面必须出现 metric_refs/DeepSeek”等旧断言不适用于本次界面，当前入口为下列命令。

## 复现

```bash
npm run build --prefix 05_原型/frontend
node 05_原型/tests/e2e_incremental_20260916.mjs
```

服务由项目启动脚本启动，默认地址 http://127.0.0.1:8765。测试支持 PHARMA_E2E_URL 与 PHARMA_CHROME_PATH，优先复用 Playwright 安装缓存，再检查既有 WSL Chrome 缓存。下载目录为 /tmp/pharma-browser-downloads。

## 证据与限制

- results.json：最终每项状态；history.jsonl：保留每次尝试；frontend_manifest.json：本次源文件摘要。
- downloaded_report.docx / downloaded_report.pdf / downloaded_machine_audit.json：真实浏览器下载字节；browser_trace.zip：最终浏览器跟踪。
- ACCEPTED/received 的界面分离检查使用明确标记的只读 API 注入样例；断网检查使用 route.abort 模拟网络失败，均未冒充真实发送。
- 超时查询、远端重启、payload 冲突和模拟通知发送由后端独立测试覆盖；本浏览器不代替用户确认发送。
- 不把机器看图替代真人归因 0–5 分、可读性或版式评审，三者仍待人工完成。
- 浏览器跟踪、截图、下载附件含题包业务数据，归参赛队内部交接材料，不能直接放公开源码候选包。

## 最终截图

- [2026-09-15T18-48-47-842Z_1366_cost.png](2026-09-15T18-48-47-842Z_1366_cost.png)
- [2026-09-15T18-48-47-842Z_1366_chart_tooltip.png](2026-09-15T18-48-47-842Z_1366_chart_tooltip.png)
- [2026-09-15T18-48-47-842Z_1366_benchmark.png](2026-09-15T18-48-47-842Z_1366_benchmark.png)
- [2026-09-15T18-48-47-842Z_1366_evidence.png](2026-09-15T18-48-47-842Z_1366_evidence.png)
- [2026-09-15T18-48-47-842Z_1366_quarter.png](2026-09-15T18-48-47-842Z_1366_quarter.png)
- [2026-09-15T18-48-47-842Z_1366_action_draft.png](2026-09-15T18-48-47-842Z_1366_action_draft.png)
- [2026-09-15T18-48-47-842Z_1366_acceptance.png](2026-09-15T18-48-47-842Z_1366_acceptance.png)
- [2026-09-15T18-48-47-842Z_1920_cost.png](2026-09-15T18-48-47-842Z_1920_cost.png)
- [2026-09-15T18-48-47-842Z_390_cost.png](2026-09-15T18-48-47-842Z_390_cost.png)
- [2026-09-15T18-48-47-842Z_1366_controlled_failure.png](2026-09-15T18-48-47-842Z_1366_controlled_failure.png)
