# 已批准的仓库清理申请

**本次用户已批准 M01/C02/C05/L01，2026-09-19 已按大小/哈希核对、外部归档后执行。旧审计内容保留为批准前背景。** 固定核对开发提交cdd9cb9和原main提交acc4693。本轮已完成的文档与索引修订不删除文件；本清单也不是可直接执行的授权脚本。

## 推荐本次批准的范围

| 组 | 具体对象 | 原因与保留方案 |
|---|---|---|
| M01 | 将已审计开发成果正常整合到main；main中993个旧路径将按队友已有提交退出当前树 | main仍停旧版，含team_internal、旧评测/报告、孤立入口。并非逐个抹掉commit；共享祖先和原件仍保留。完整旧路径见main_only_inventory.tsv |
| C02 | 02_知识库/00_原始内7份额外PDF，共3,385,170字节 | 与PACKAGE/03_制药知识文档原件逐字节一致。保留原ZIP及真正运行来源；先保留来源映射和可恢复归档，再移除额外副本 |
| C05 | demo_20260918内另绘阅读PDF及slide-1.png至slide-10.png，共11份、10,939,230字节 | 保留原PPTX和pptx_render内原生PDF/PNG作为一致的阅读入口；另绘版本先归档，不能声称其完全无独有排版信息 |
| L01 | 本轮函数导入产生的7份本地未跟踪.pyc缓存 | 不在Git，不含源代码；仅清理本次列明文件，不删除整个__pycache__目录或其他环境 |

C02+C05合计18个现存跟踪文件、14,324,400字节（约14.32 MB）。该体积不等于Git历史压缩后可回收空间。M01的993个旧路径属于main与开发分支差异，已与上述18个当前文件分开统计，不能混成“最新分支有1011个垃圾文件”。

精确路径、原始哈希、大小和操作边界见[deletion_request.json](deletion_request.json)；全部候选与依赖见[cleanup_inventory.md](cleanup_inventory.md)、[cleanup_candidates.tsv](cleanup_candidates.tsv)。main旧路径见[main_only_inventory.tsv](main_only_inventory.tsv)。

## 本次明确保留

保留10份仍被回退输入接口引用的CSV（C01）；保留30份独有历史数据/解析索引（C03），仅做历史标识；保留旧release三份演示（C04），它们有合成迁移价值且属于不可变交付清单。原始ZIP、净化原件、两款字体及OFL、源码、测试和所有独立历史验收均保留。无需为了目录简短删除有用能力。

不删除任何commit、分支或标签，不重写历史，不force push，不修改可见性。若用户仅批准部分组，其他组继续保留。

## 批准后的执行条件

先固定新的远端版本、保护队友后续提交；清单路径内容发生变化就重新核对，不能扩大为目录通配删除。先完成可恢复归档、原件字节核对及来源映射。移除C02前回归当前知识来源；处理C05时更新生成/跟踪规则、现行媒体清单，保留历史原SHA记录。最后正常合并main，核对实际删除路径仅限M01及获批C组，运行受影响的检查并报告远端提交。

本次批准不包含未来全目录改名、未列出的素材删除或历史敏感内容擦除。共享历史是恢复和协作依据，“旧提交”本身不等于应该删除的备份。


2026-09-19 治理更新：C02 的 7 份知识 PDF 使用 PACKAGE 原件，路径映射见 `docs/repository/deduplication_mapping.json`（Git 根相对路径）；CSV 与原 ZIP 保留。C05 另绘阅读 PDF/10 张 PNG 已归档移除，当前阅读使用 `pptx_render` 原生渲染；旧阅读版身份保留于媒体历史清单。默认生成仅 PPTX，`--reading-preview` 可选预览输出到忽略的 `_reading_preview/`。
