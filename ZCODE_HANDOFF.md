# ZCode 接续交接：GLM-5.3主开发与Flash视觉审阅

## 2026-09-16修订：职责、顺序与决定权

本轮按实际源码包重新审计后明确开发分工：**GLM-5.3承担全部重要非视觉工作和所有正式代码修改；GLM-5.3-Flash只承担必须看图的观察与复查。** 前端、Word/PDF、PPT并非整体属于视觉模型。代码、正文逻辑、业务判断和验收实现均由5.3负责，Flash提供可定位的视觉证据，由5.3据此修复。

此分工替代旧交接中的通用探索prompt及旧Agent所有权分配，其他有效要求保留。它用于开发协作，**不自动改变应用默认glm-5.3-flash或引入业务运行时多模型架构**。本文和两份prompt已更新，应用源码尚未按本轮新发现修复。

| 任务 | GLM-5.3主责 | Flash边界 |
|---|---|---|
| 赛题、需求、架构、研究与创新实验 | 设计、决定、实现、验收 | 不分派 |
| 数据、公式、RAG、模型/API、RPA、安全和故障恢复 | 全部负责 | 不分派 |
| 业务正文、专业措辞、演示脚本 | 全部负责 | 只报告视觉排列导致的误读 |
| React/TypeScript/CSS/ECharts、Word模板及导出代码 | 全部编写、修复 | 不修改代码 |
| DOM/可访问性树、控制台、自动测试、截图与页图生成 | 全部负责 | 复用这些产物 |
| 图片中的漏字、遮挡、层级、对齐、分页和图表可读性 | 接收观察并实施修复 | 实际看图、提出建议、复查 |
| 最终打包、状态汇总和技术验收 | 唯一集成人 | 只给限定范围的视觉结果 |
| 真人归因0–5分、专业阅读和最终签收 | 实现记录入口，等待真人 | 不能代签 |

5.3可以运行浏览器、读取DOM和生成截图；这些动作不要求它具备视觉输入。5.3不得把Flash描述或工具执行成功说成自己直接看过图片。Flash提出视觉建议后由5.3判断业务影响并改代码，避免两个模型维护冲突实现。

## 本次新确认的优先接续任务：全部交给5.3

1. **安全恢复。** 原prepare_team_workspace.py重复执行会覆盖队员修改，已隔离复现。先修成缺失复制、相同跳过、冲突保留并记录；已准备的工作区直接接续，不再次无条件合并。
2. **新前端没有构建。** 包内带dist，bootstrap依据dist/index.html存在跳过npm安装/构建。修改src后仍可能展示旧界面，已隔离复现。按源码、配置与锁文件变化保证实际服务新构建，不要求每次启动重装。
3. **验证不可移植。** verify.py读取baseline.files，但打包生成扁平路径映射；旧清单与旧job也不属于新机runtime。统一基线合同、PHARMA_PYTHON入口和新run清单，测试从空runtime恢复后生成并验证。环境检测补兼容版本与关键能力；外置模型只读复用，不覆盖其他项目资产。
4. **模型解释误通过。** 仅一条summary数字finding即可得到PASS/llm，而材料等解释仍规则回退，已最小复现。按具体分析任务核验必要解释覆盖，合法且针对性的缺证结论可以成立，但数字复述不能冒充完成归因。
5. **请求型号冒充响应型号。** 生产网关主要记录请求model，需每次保存并核验returned_model及响应结束情况，不能用一次探针代表所有报告；用量应汇总修复调用或说明范围。
6. **人工审核缺入口。** assess_report把章节实质、阅读、视觉固定PENDING，API未提供审核录入与重算。补最小版本绑定记录，包含审核人、时间、评分、意见；产物变化使相应审核失效。程序完成、规则降级、视觉审阅和业务审核分别表达，不能代签或仅手改PASS。

原包401项内部文件校验本轮全部匹配，旧单独MD与包内同文件一致。但包内unpack_acceptance对应的外层ZIP与本次上传ZIP不同；不能据旧回执确认当前ZIP已经完成启动验收。原回执还明确复用同机解释器与外置embedding，不代表队员净环境部署。保留旧回执，后续对最终实际ZIP另作验收；不要改写历史身份让它看似匹配。

旧问题已有大量修复：空建议、业务JSON、同比缺项、比较贡献率和S2错误产品证据已有新代码/报告支持，不再机械重做。历史记录为70项Python回归、5574项独立数值观测、999项报告复核、48项应用浏览器、18项方案浏览器和9项RPA检查；这些不是本轮重新执行的结果。四报告现为6/5/6/6页，共23页，仍因MODEL_KEY_NOT_SET为基础分析DEGRADED。

## 最小协作闭环

优先用glm-5.3主会话和固定glm-5.3-flash的项目专用视觉子智能体。ZCode自定义子智能体是Beta，先核对实际账号/版本、模型通道、图片读取和结果回传；官方字段为model和thoughtLevel，配置更新需新会话，不能宣称热加载。不要改变影响其他项目的内置默认角色，也不杜撰CLI命令。先用一张合成图确认Flash真正接收到图像。

不可用时使用两个会话共享文件。共享目录不保证自动唤醒，如需队员触发应明确最小操作；5.3继续无依赖工作，不空转轮询。

- 5.3写06_评测/dual_model/<run_id>/visual/<task_id>/request.json，指定目标、场景、输入图像、产物版本、视口、检查范围和结果目录。输入为冻结副本。
- Flash只返回观察，或写该目录result.json及review.md；不改请求、主状态、源码、业务数据或测试期望。
- 问题包含稳定issue_id、画面位置、观察、读者影响、严重性、最小建议和复查方式。结果为PASS_VISUAL、NEEDS_FIX或BLOCKED，不代表功能和数字通过。
- 5.3修改代码、重新生成产物再交Flash复查。版本变化使相关旧结果过期；最终报告须全部页覆盖，字体/分页全局变化重查受影响全体页面，局部变动不重复无关检查。
- 主状态、服务启停、安装/下载、共享配置和最终集成由5.3唯一协调。其他非视觉子任务如需并行，也使用5.3并明确文件范围。

可先让Flash对现有23页和最终浏览器截图作V0基线观察，同时由5.3修独立代码问题；正式终检以新构建为准。真人归因、可读性和最终签收仍由人完成，两种模型均不能冒充真人。

以下原交接细节继续作为背景；凡与上述新发现冲突，以新发现及实际源码为准。详细审计见docs/双模型接续审计.md。

## 当前事实与入口

这是一项对现有初版的增量修复。源码仍在 `05_原型`，没有另起项目。原项目没有 Git；恢复基线保存在原工作机 `/home/hujunjie/pharma_recovery_baselines/20260916T022230/`，含完整归档和逐文件 SHA256。原始题包、原模板、历史 DeepSeek 成功/失败证据没有改写。

用户已澄清：没有另外提供的 `02_audit` 交接包，本轮依据当前 WSL 项目与历史证据执行；交接包是本轮输出。开发工具 ZCode CLI 位于队员电脑，本工作机运行的是 Codex，不能宣称已在 ZCode 中执行，也不能杜撰其命令参数。

## 在队员电脑先做什么

1. 用**已安装 ZCode 的实际入口**打开交接包恢复后的项目目录，先读 `AGENTS.md`、本文件、`docs/INCREMENTAL_AUDIT.md` 和本轮 `verification.json`。不要重新创建工程；AGENTS中的原作者技能路径与恢复路径须按当前机器确认。
2. 如果只解压公开源码候选包，它不含题包与报告；由5.3先修上述安全合并问题，再将team_internal合并进工作副本。不要在已修改目录直接重复运行旧prepare脚本。不能把公开候选包当成独立全数据系统。
3. 先修上述环境检测、解释器与构建缺陷，再运行 `05_原型/scripts/check_environment.py`、bootstrap/start。依赖只装到本项目 venv；已有兼容解释器可通过 `PHARMA_PYTHON` 显式复用，探针/验证/场景脚本统一使用该解析结果，不能始终硬编码.venv。默认 `127.0.0.1:8765`，原题包 mock 为8090；端口冲突可用 `.env` 的项目专用端口配置。不得终止其他项目服务。
4. 检索模型缓存没有打包。`fetch_embedding.py` 仅下载缺失的固定版本模型/tokenizer，并验证清单SHA；网络不可用时，设置 `PHARMA_EMBEDDING_DIR` 指向已授权、哈希匹配的现有资产。没有语义资产时混合检索不能标通过。
5. 本机原Noto CJK字体经LibreOffice导出为Type1子集后出现缩放相关漏绘，已补项目内Noto Sans SC TrueType Regular/Bold及SIL OFL许可证（05_原型/assets/fonts）。Linux导出以临时Fontconfig配置加载，不改系统字体；Windows Word打开时安装包内同字体。仍须在队员电脑实际检查LibreOffice/浏览器/Word渲染，不能批量转码。

## 首要阻塞：GLM应用API真实验证

**当前真实结果为 BLOCKED / MODEL_KEY_NOT_SET**。已配置默认 `glm-5.3-flash`，没有用 `glm-5.3`、旧DeepSeek或规则输出来冒充GLM成功。

- 开发Agent的Coding Plan与应用运行时API是两件事；无限开发token额度不证明应用调用免费。
- 中国站通用候选：`https://open.bigmodel.cn/api/paas/v4`；国际站通用候选：`https://api.z.ai/api/paas/v4`。先按账户和当日官方文档核验，不用包含 `/coding/` 的端点。
- 官方依据：[Z.AI Chat Completion](https://docs.z.ai/api-reference/llm/chat-completion)。当日本次读取的文档注明GLM-5.3-FLASH仅支持启用思考，`reasoning_effort` 为 low/high/max。网关未照搬DeepSeek的disabled，默认low，输出预算8192。官方通用模型列表未完整列出此精确型号，账号可用性必须实调确认。
- 密钥用受控文件，`.env`只填写 `PHARMA_MODEL_KEY_FILE` 路径；亦支持环境GLM_API_KEY/ZHIPU_API_KEY。禁止粘贴到聊天、截图、日志、代码或交接包。
- 运行 `05_原型/.venv/bin/python 05_原型/scripts/probe_glm.py --output 06_评测/新运行目录/glm_probe.json`。探针仅发送极小连接测试内容，分别检查文本、JSON、工具调用、SSE流式和返回的精确模型ID。不得将HTTP200当成结构与身份通过。
- 真实限流/超时目前未观测；本地HTTP夹具覆盖失败降级。核验账户配额、应用费用、429/Retry-After与超时表现，避免用压力攻击制造429。没有验证就保持待检查。
- 探针通过后，重新生成 S1银黄5月、S2板蓝根5月专题、S3六味3月、Q2银黄4—6月季度。使用 `generate_scenarios.py --output 06_评测/新运行目录/scenario_reports.json`，不要覆写本次失败证据。检查章节级model参与/repair记录和实际usage。
- 报告默认基础版是有价值的确定性分析，但模型参与未通过；禁止默退DeepSeek、放开模型自算或删除数字校验。

## 真人必须接续的审核

四场景 DOCX/PDF 已按本轮记录逐页渲染与Agent检查；这**不等于真人验收**。对每一场景填写：

- 归因合理性0—5分、审核人、日期、评语，明确事实/假设/建议。
- 内容可读性、全部页版式（目录页码、标题连排、表格续页、图例单位、字体缺字和裁切）。
- 章节实质完整和证据支持程度；尤其二厂原料明细仍缺，不能推算填补。
- 实際业务负责人分配任务和日期；发送必须确认当前完整载荷，模拟通知不等于真实整改完成。

所有强制维度必须通过才可将报告总体验收改PASS。当前文件打开、计算、业务内容、证据、阅读、视觉、任务和模型分别记录；不要恢复旧“DOCX/PDF PASS即合格”的表述。

## 检索后续探索边界

v4固定规则在历史已见集混合Recall@5为0.928571、BM25为1.0。该集和原holdout已经被检查，不能再称新保留集或据其反复调参后宣称泛化。建立**新未见问题集**并先冻结：产品串扰、跨期事件、文档版本冲突、无证据问题。比较语义/BM25/混合分项；当前仍不能宣称混合优于BM25。每次解析版本变更重新建索引，禁止沿用旧版本结果伪装当前质量。

## 两份执行prompt

- 主会话使用 `docs/prompts/GLM53_主开发与集成.txt`。
- 视觉子智能体或独立Flash会话使用 `docs/prompts/GLM53Flash_视觉审阅.txt`。
- 先启动主会话；Flash按主会话任务执行，或在主会话并行修复期间先审冻结的V0基线。5.3负责继续接收、修改、复查与交付。

官方依据：[GLM-5.3文本模态](https://docs.bigmodel.cn/cn/guide/models/text/glm-5.3)、[Flash视觉能力](https://docs.bigmodel.cn/cn/guide/models/vlm/glm-5.3-flash)、[ZCode子智能体](https://zcode.z.ai/cn/docs/subagents)。
