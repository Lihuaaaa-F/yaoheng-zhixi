# 当前交接包评估与双模型职责

审计日期：2026年9月16日。结论：可以作为继续开发的基础，但尚不能作为最终比赛成品，也不能认定已经完成队员机器部署。

本轮读取了原ZIP、清单、源码、脚本、测试及验证记录，核对401项包内文件一致性；核对单独MD与包内同文件一致。抽查当前四份PDF首页、S2末页和两张最终浏览器截图，并读取四报告文本。针对准备脚本覆盖、旧前端构建未更新和模型解释误通过做了隔离最小复现。没有登录用户模型账号、启动完整应用、安装依赖或完整重跑包内70项测试；本轮只更新交接说明和提示词，不修改应用代码。

## 当前已完成部分应保留

上轮的业务JSON打印、空建议/任务、S2错误产品工艺、同比要素缺项与贡献率基期混用已获得当前源码和输出支持的修复。S2从16页变为5页；四报告共23页，分别6/5/6/6页，新的报告包含有数据支持的原料驱动、预算分解、跨厂三要素和具体核查建议。不能再要求队员机械修复所有旧问题。

包内70项Python回归、5574项独立计算观测、999项报告复核，以及48/18/9项应用浏览器/方案浏览器/RPA记录均为历史运行证据。四报告仍DEGRADED，GLM实调因缺应用密钥BLOCKED；真人评分待评。这一状态标注比旧文件PASS更准确。

## 对下一阶段有直接影响的缺陷

| 优先项 | 依据与影响 | 负责人和验收 |
|---|---|---|
| 重复准备覆盖改动 | prepare_team_workspace使用copytree并允许覆盖；隔离复现确认队员修改被旧文件替换 | 5.3修安全合并；重复执行后用户修改不丢失 |
| 前端源码与运行界面脱节 | bootstrap只看dist/index.html是否存在；包内自带dist，修改src仍可能跳过构建 | 5.3修构建输入判断；实际服务产物对应新源码 |
| 验证入口不兼容 | verify读取baseline.files，当前baseline是扁平映射；还依赖旧清单和旧job数据库 | 5.3统一合同、新run参数及恢复路径；空runtime恢复后可验证新产物 |
| 解释内容少仍判模型通过 | 假网关仅返回summary数字事实，实际generate仍PASS/llm，材料等章节均规则降级 | 5.3按任务定义解释覆盖；summary-only反例不能通过完整模型解释验收 |
| 模型身份记录不可靠 | 生产网关使用请求model记录结果，未逐调用验证响应model；最终usage可能仅最后一次 | 5.3区分请求/响应身份和调用范围；不得冒名或漏记修复用量 |
| 人工审核无法正式完成 | assess_report的人类维度常量PENDING，缺审核录入和重算入口 | 5.3实现最小记录；真人签收绑定版本，修改后失效，模型不得代签 |
| 证据状态聚合过粗 | 证据适用性维度依赖整体状态和“已检查”布尔，未说明逐结论覆盖 | 5.3按实际claim验证结果聚合；区分数字事实、证据假设和合法缺证 |
| 新机器可运行性尚未证明 | 包内解压回执对应另一外层ZIP，且复用原机解释器与模型 | 5.3对最终实际ZIP重新验收；历史回执原样保留 |

环境检查仅import成功不足以证明版本兼容，PHARMA_PYTHON在各脚本中的使用也不一致；外部模型路径不可下载覆盖为新资产。这些都是非视觉工程工作，应由5.3处理。并行开发时只保留一个环境/服务/配置协调者，Flash不启动共享服务或修改构建。

## 当前视觉状况及Flash真正需要做的事

抽样页已显示明显改善：业务分析进入首页，正文没有旧JSON大段污染，字体可见且建议不再为空。仍可观察到首页重复期间、较长机器式报告编号、目录挤成段落、紧密表格和建议区较高阅读密度。前几项可由5.3先从文本与生成代码判断，字号、行宽、层次及读者视觉负担再由Flash看图反馈。

本轮抽样不能替代四场景所有页和队员机器的全量终检。历史23页审图记录只对应历史产物；后续修改字体、正文或分页后必须检查新版本。Flash应对最终页图给定位明确的问题，不以“前端”或“文档”关键词接管代码和内容论证。

## 对用户分工设想的优化

官方明确GLM-5.3当前仅处理文本，Flash支持图像与视频输入，这足以支持按模态分工。官方材料并不能证明Flash在所有非视觉任务上都更差，因此不把这种绝对强弱判断作为验收依据。[GLM-5.3](https://docs.bigmodel.cn/cn/guide/models/text/glm-5.3)、[GLM-5.3-Flash](https://docs.bigmodel.cn/cn/guide/models/vlm/glm-5.3-flash)。

推荐5.3负责全部正式实现，Flash只负责必须看图的观察和复查。5.3仍能编写浏览器测试、读取DOM、生成截图、修CSS和Word排版代码。将视觉反馈转成代码修改和判断业务风险，由5.3完成。这样既利用视觉能力，又避免共享文件冲突及责任分散。

优先在ZCode中使用固定Flash模型的专用子智能体，主会话保持5.3；官方支持指定model、thoughtLevel与工具，功能仍为Beta，配置变更后需要新会话。实际不可用时再用双会话文件交接，不假定共享文件能自动唤醒对方。[ZCode子智能体说明](https://zcode.z.ai/cn/docs/subagents)。

本轮对“模型分工”的理解是开发会话分工。业务应用继续保留已配置的glm-5.3-flash，不顺带增加运行时多模型服务；如要改变应用默认，应另据业务测试和账户条件决定。开发套餐与应用API仍分别核验。[ZCode模型配置](https://zcode.z.ai/cn/docs/configuration)。

## 高分方向与停止条件

5.3负责探索与取舍，优先强化“适用证据—具体解释—核查动作—重新验证”。新的模拟数据不能冒充题包事实，缺采购价/实耗/二厂明细不能编造严格因果；Flash不负责财务归因评分。混合检索在已见集0.928571仍低于BM25的1.0，应诊断并做新题对照，不盲目堆框架。

达到既定功能、模型、数字/证据、视觉及真人要求后停止可选优化；不能因为token充足无限扩展范围。未取得有效官方评分权重和对手作品，无法计算获奖概率。比赛提交、公开仓库、视频/PPT等交付仍须按赛题与授权完成，而不是由模型自行宣布高分。

执行顺序与两个完整prompt见ZCODE_HANDOFF.md及prompts目录。详细开发审计和复现摘要在audit目录供追踪，不应复制到业务报告正文。
