# 公开资料边界

仓库本轮访问时为public；目录名public_source_candidate/team_internal不是权限隔离。当前树已逐路径移除题包原文、CSV原数据、知识原件/索引、原工作模板、完整派生报告/截图/日志、历史数值golden/测试和含私料数字的旧方案工具跟踪。精确Git路径见privacy-removals.json，固定基点acc469312c8366e4b0438e702a5f435779e7a125。

原件未删改。取得可信clone、确认无本项目运行进程后，旧项目整体移到工程外恢复点，并独立保存上游工作树私有快照；运行队列未重新启用，待发任务隔离。哈希备份校验和本机恢复绝对路径只在工程记录，不放用户业务报告或公共仓库。

本机私有回归仍可访问保留原件；公开测试使用独立合成样例。恢复以PHARMA_DATA_PACKAGE指定外部原题目录；不将旧源码、dist或整份runtime覆盖回来。prepare仅originals模式可恢复白名单资源，冲突保留incoming副本；开发Git树无需旧发行清单核验。

## 仓库负责人后续

当前删除不能清除acc4693及更早Git提交、克隆、fork、缓存或下载副本。负责人需核对赛题资源授权、识别既有副本范围并按平台流程处理；如决定重写历史或改变可见性，需另行明确授权及团队协调。本轮未强推、未重写历史、未更改可见性，不能声称历史泄漏解决。

公开交付检查：只暂存代码、文档、锁文件和独立合成包；检查tracked/staged而非仅gitignore。公开打包只允许审计后Git HEAD，不自动夹带team_internal。本地完整产物与恢复备份不上传。
