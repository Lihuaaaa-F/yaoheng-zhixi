# 最终运行答辩材料

当前报告与媒体绑定：`delivery_20260919_final`，受测代码 `04011c2`。

模型新调用来源为 `delivery_20260919_26`；本轮七场景复用此前已验证的模型缓存并生成最终报告，未新增模型 HTTP 请求，两轮合计仍为 7 次。PPT 内模型身份沿用已验证事实，不代表本轮重新调用模型。

- `药衡智析_演示与答辩稿.pptx`：10 页答辩稿，S1/S2/S3 数值、报告预览及模拟 RPA 结果来自最终运行。
- `pptx_render/`：实际 PPTX 经 LibreOffice Impress 原生渲染的 PDF 与 10 张 PNG。
- `native_render_check.json`：运行、尝试、代码、场景与产物绑定，以及文字、字体、页界和视觉核验结果。

默认关闭另绘阅读预览。真人专业归因、可读性、版式意见和署名评分保持 PENDING；自动核验不代表比赛整体验收。
