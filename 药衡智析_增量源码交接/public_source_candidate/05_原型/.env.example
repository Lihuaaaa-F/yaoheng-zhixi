# 应用运行时配置；复制为本项目 .env，禁止提交/打包真实密钥。
# 统一解释器解析顺序：PHARMA_PYTHON → 项目venv → 系统python（bootstrap/start/stop/verify/probe/场景生成一致）。
# PHARMA_PYTHON=/可选/已装齐兼容依赖的既有解释器
#
# —— 模型配置（2026-09-22 三模块改版）——
# 三个角色：analysis=数据分析模型（大模型，报告/归因/任务生成，赛题基础项）；
#           extraction=数据提取模型（小模型，字段映射等轻量任务，赛题加分项）；
#           vector=向量模型（本地 ONNX，仅在“模型配置·向量模型”页切换，无 API 选项）。
# 页面（模型配置）与这里的环境变量读同一份配置；环境变量优先。厂商预填充
# （智谱/DeepSeek/通义/Kimi/硅基/OpenAI/本地 Ollama·vLLM·LM Studio）见
# GET /api/settings/models/presets，数据来源 backend/pharma/model_registry.py。
PHARMA_MODEL=glm-5.3
PHARMA_MODEL_PROTOCOL=openai
PHARMA_MODEL_BASE_URL=https://open.bigmodel.cn/api/paas/v4
# 数据提取模型（小模型）：未配置时与主模型同源回退，导入流程确定性可用。
# 旧名 PHARMA_MODEL_DECISION_MODEL / 路由键 decision 仍兼容（自动映射 extraction）。
# PHARMA_MODEL_EXTRACTION_MODEL=glm-4.5-air
# 推理强度：low/medium/high（页面为滑块）；各厂商请求参数映射见 model_registry.effort_body_params。
# PHARMA_MODEL_REASONING_EFFORT=low
# 档位限制：extraction 拒绝已知旗舰档（如 glm-5.3），analysis 拒绝已知轻量档
# （如 glm-4.5-air）——赛题“报表生成用大模型、数据提取用小模型”。
# 可选 API 鉴权：本地演示（默认仅 127.0.0.1）无需设置；部署到局域网/共享环境
# 时设置随机长 token，所有 /api/* 请求须带 X-API-Token 头（浏览器前端为本地
# 演示形态，启用 token 后需要经代理注入头）。
# PHARMA_API_TOKEN=
# 中药二厂合成明细（按题包汇总精确校准、全程标注"合成演示数据"）：2026-09-22
# 起默认停用——对标拆结构按题包真实汇总口径，二厂缺明细时输出“证据支持假设/
# 证据不足”归因推测。评测对照需要时显式设为目录重新启用；
# 生成脚本：05_原型/scripts/generate_synthetic_plant2_details.py
# PHARMA_SYNTHETIC_DETAIL_DIR=/目录/synthetic_detail_中药二厂
# 国际账号候选：https://api.z.ai/api/paas/v4，需同账号实测。
# 端点回退（用户2026-09-18授权暂定政策）：主端点先消耗账户余额/资源包；
# 错误码1113（额度耗尽）时自动切换 Coding Plan 端点完成调用，主端点充值
# 后自动恢复优先，无需改代码。置空可禁用回退。
# PHARMA_MODEL_CODING_BASE_URL=https://open.bigmodel.cn/api/coding/paas/v4
# PHARMA_MODEL_KEY_FILE=/受控目录/glm_application_api_key.txt
PHARMA_MODEL_MAX_CALLS=40
PHARMA_MODEL_MAX_REPAIRS=2
# 缺凭据时保留基础分析；不会自动改用DeepSeek。
# 多模型协作（赛题加分项）：JSON 路由表（route→{model,base_url,protocol,key_file}），
# 键名用 extraction/analysis（旧名 decision/narrative 自动映射）。
# PHARMA_MODEL_ROUTES={"extraction":{"model":"glm-4.5-air"}}
# 未配置的路由自动回退主模型配置，机制透明不伪造多模型实调。
# 可选资产路径（不填则使用项目默认路径）
# PHARMA_DATA_PACKAGE=/队内资料/创灵境_考题模拟数据
# PHARMA_EMBEDDING_DIR=/模型资产/bge-small-zh-v1.5（向量模型页“确认”切换时若
# 该变量已设置会拒绝写入，因环境变量优先级更高）
# PHARMA_API_PORT=8765
# PHARMA_RPA_PORT=8090
# 合成演示上下文（synthetic-pharma/机械/化工参考包）默认不进数据范围目录；
# 测试/迁移校验置 1 可列出。
# PHARMA_SHOW_TEST_CONTEXTS=1

# 自动重点解释（赛题 5.2.3）：要素环比严格超过±10%自动生成重点分析段落并
# 入队模型解释；2026-09-22 起默认开启（同版本幂等复用，失败须手动重试）。
# 置 0 关闭。
PHARMA_AUTO_EXPLAIN=true
