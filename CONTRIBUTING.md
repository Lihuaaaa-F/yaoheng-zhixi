# 协作与提交

## 找到当前工作版本

先阅读根 README、docs/repository/DELIVERY.md、应用 docs/current_run.json 与 docs/ZCODE_HANDOFF.md。区分 Git 根和应用根；只把已绑定的运行记录称为当前记录。

开工先查看 `git status`、远端和当前分支，再 `git fetch origin`。未提交内容先妥善保存；有团队新提交时有选择地合并并验证，不把 `--autostash` 当作完整备份，不强推已共享历史。日常使用任务分支，经审阅后整合到 main。

## 修改边界

保持通用核心、行业包、企业配置和技术适配器的分界。行业变动优先修改包/配置，核心接口不足时附最小反例及兼容改法。GLM-5.3承担重要非视觉开发，Flash按需检查图像，真人评分由真人提供。

金额、数量、比率、单位和政策的合同不可通过宽松校验规避；模型不改数字，送达不等于完成。修复应验证真实失败场景，复用现有测试，不能用被测函数生成黄金答案。

## 提交要求

- 提交聚焦，说明问题、修改、影响与实际验证。只暂存确认过的路径；检查新文件及敏感内容。
- 密钥、环境、运行数据库、队列和备份不入库。赛题资源沿用既有范围，未来企业资料单独确认。
- 文档修改运行 `python3 tools/verify_repository.py`；源码修改另运行受影响的应用测试与构建，不能用仓库检查代替。
- 每份模型/检索/RPA/浏览器回执应绑定 run、代码、场景、job及相应产物版本；没有执行就写未执行。
- 静态报告、演示和最新模型生成可以来自不同批次，但必须明确来源；不覆盖历史失败为PASS，不代填真人评审。

## 删除与整理

用户当前要求：任何文件删除、移除Git跟踪、旧路径迁移移除、分支/标签删除及历史清理，均先形成精确清单，说明理由、依赖和保留方案，统一取得批准后执行。普通README/文档内容修订不删除文件，本次按此边界完成。

不因提交过时而删除commit；正常Git历史是协作恢复依据。当前树清理、归档和敏感历史重写是不同工作。禁止 `git push --all`、强推、无审查 `clean -fdx`，禁止把待批准清单当成已批准脚本。
